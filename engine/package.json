{
  "name": "ai-engine",
  "version": "1.0.0",
  "description": "AI Engine for cockpitAI — BullMQ worker dispatching tasks to CLI agents",
  "type": "module",
  "main": "aiEngine.js",
  "scripts": {
    "start": "node aiEngine.js",
    "dev": "node --watch aiEngine.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --testTimeout=15000"
  },
  "dependencies": {
    "bullmq": "^5.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.21.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "ioredis-mock": "^8.9.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "transform": {}
  }
}
